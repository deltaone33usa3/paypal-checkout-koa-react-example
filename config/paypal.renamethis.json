{
    "client_id": "YOUR CLIENT ID HEREcurl -v POST https://api.sandbox.paypal.com/v1/oauth2/token \
  -H "Accept: application/json" \
  -H "Accept-Language: en_US" \
  -u "CLIENT_ID:SECRET" \
  -d "grant_type=client_credentials"",
    "secret": "YOUR SECRET HERE",
    "environment": "sandbox"
}
